import React from 'react';

test('renders learn react link', () => {
  // Todo: Setup jest properly
  // const { getByText } = render(
  //   <Provider store={store}>
  //     <App />
  //   </Provider>
  // );
  //
  // expect(getByText(/learn/i)).toBeInTheDocument();
});
